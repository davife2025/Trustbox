{
  "name": "@trustboxhedera/shared",
  "version": "1.0.0",
  "description": "Shared types, config, and ABIs for TrustBoxHedera AI",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
